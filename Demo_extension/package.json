{
  "name": "ai-models-bridge-demo",
  "version": "1.0.0",
  "description": "Demo extension for AI Models Bridge",
  "main": "popup.js",
  "scripts": {
    "build": "npm install && webpack",
    "buildA": "webpack",
    "dev": "cd ../Library && npm run build:demo && cd ../Demo_extension && npm run build",
    "devA": "cd ../Library && npm run build:demoA && cd ../Demo_extension && npm run buildA"
  },
  "keywords": [],
  "author": "Sukarth Acharya",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}